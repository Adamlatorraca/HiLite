<h1>New Category</h1>

<%= render partial: "shared/errors", locals: {object: @category} %>

<%= form_for @category do |f| %>
    <%= f.label :name %>:
    <%= f.text_field :name %>
    <br>
    <%= f.submit %>
<% end %>